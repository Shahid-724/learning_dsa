# This code takes a string as input
# Prints the unicode values of all chars

# Taking input from the user
word = input('Enter a string: ')

# Printing the output
for char in word:
    print(ord(char))