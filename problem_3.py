# This code takes a string as input
# Copies the string into another variable and counts the no of chars

# Taking input from the user
str_1 = input('Enter a string: ')

# Declaring a variable to copy the string
str_2 = str_1

# Printing the copied string and length
print(str_2)
print(len(str_2))