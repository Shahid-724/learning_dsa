# This code reads a number in the form of a string
# The number consists of only two digits
# Print the digits in reverse order

# Taking input from the user
num = input('> ')

# Printing the digits in reverse order
print(num[1] + num[0])