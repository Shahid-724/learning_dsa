# This code reads a word as input
# Prints the first and last characters in seperate lines

# Taking input from the user
word = input('> ')

# Printing the first and last characters
print(word[0])
print(word[len(word) - 1])